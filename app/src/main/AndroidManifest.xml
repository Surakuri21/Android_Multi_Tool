<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Caerlangs_calculator">

        <!-- DASHBOARD IS THE ONLY MAIN LAUNCHER -->
        <activity
            android:name=".DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ONLY THE 5 STABLE TOOLS ARE REGISTERED HERE -->
        <activity android:name=".CurrencyActivity" android:exported="false" />
        <activity android:name=".PowerActivity" android:exported="false" />
        <activity android:name=".TemperatureActivity" android:exported="false" />
        <activity android:name=".NumberActivity" android:exported="false" />
        <activity android:name=".MultiplicationActivity" android:exported="false" />

    </application>

</manifest>